<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 网络请求 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        位置权限：用于「附近影院推荐」「按距离排序」「定位当前城市」。
        FINE_LOCATION 用于在地图/距离场景下提供更准确的排序；
        COARSE_LOCATION 作为兜底（用户拒绝精确位置时也能给出粗略结果）。
        Google Play Data Safety 中需如实勾选"精确位置 / 粗略位置 - 仅用于 App 功能 - 不与第三方共享"。
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 相机权限：用于拍摄头像、反馈或评论图片（image_picker 触发，用户主动选择） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!--
                X (Twitter) OAuth 2.0 PKCE 回调由 flutter_appauth 自动注册的
                net.openid.appauth.RedirectUriReceiverActivity 处理，
                只需在 app/build.gradle 的 defaultConfig 里设置
                manifestPlaceholders += [appAuthRedirectScheme: 'otakumovie']
                即可。不要在 MainActivity 这里重复声明同一个 scheme 的 intent-filter，
                否则系统会弹「选择应用」对话框，破坏登录体验。
            -->
        </activity>
        <!--
            flutter_appauth 默认只按 scheme 注册回调；我们用的是 otakumovie://x-login-callback，
            必须显式声明 host，否则授权完成后浏览器无法把 code 带回 App，会卡在 X 登录页循环。
        -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:theme="@style/Theme.AppCompat.Translucent.NoTitleBar"
            android:exported="true"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="otakumovie"
                    android:host="x-login-callback" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!-- 电话：用于影院详情页跳转到系统拨号面板（不申请 CALL_PHONE，仅唤起拨号 UI） -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <!-- 邮件：用于客服 mailto: 链接 -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
