# 正式签名配置模板。复制为 android/key.properties 后填入真实值（key.properties 不入库）。
# storeFile 路径相对 android/app 目录；下例对应把 keystore 放在 android/ 下。
storePassword=你的keystore密码
keyPassword=你的key密码
keyAlias=upload
storeFile=../upload-keystore.jks
