# Git Commit 变更总结（预售券）

**范围**：仅基于当前暂存区（staged）内容生成。  
**目标**：预售券详情页与相关国际化、数据模型与组件适配的统一提交。

## 变更要点
- **预售券详情页**：顶部多图轮播、规格卡片统一高度与选中联动、特典区大图滑动+缩略图、底部购买区与页面排版完善
- **数据模型**：新增 `presale_detail_response` 类型与生成文件，页面改为强类型访问
- **组件适配**：`CustomAppBar` 增加可配置返回图标颜色
- **国际化**：补齐 `intl_*.arb`、`translations.json` 与生成的 `l10n.dart / messages_*.dart / app_localizations*`

## 建议 Commit 信息

```
feat: 新增预售券详情页与多语言支持

- 新增预售券详情页：多图轮播、规格联动、特典展示
- 如果有预售券详情，就展示预售券按钮
```
